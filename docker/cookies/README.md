# YouTube cookies

Drop a Netscape `cookies.txt` here as:

- `cookies.txt` (preferred), or
- `youtube.txt`

MusicTubearr mounts this folder at `/cookies` and auto-imports on startup.
You can also click **Import** in Settings → Metadata.

Export while signed in to YouTube (private window recommended).
